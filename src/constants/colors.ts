type Colors = {
  primary: string;
  secondary: string;
  tertiary: string;
  background: string;
};

/**
 * Constante que contém a paleta de cores usada no projeto.
 */
const COLORS: Colors = {
  primary: '#2d5d62',
  secondary: '#5e8b73',
  tertiary: '#a7c4bc',
  background: '#dfeeea',
};

export default COLORS;
